## Custom Observable

  ngOnInit(): void {
    
    // Ex - 01 (Manual)

    const custObs1 = Observable.create(observer => {
      observer.next()
      observer.error()
      observer.complete()
    })
  }


  subscribe(data, error, completion)