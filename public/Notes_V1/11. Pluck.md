    // Ex - 01
    from(this.users)
      .pipe(
        // map(data => data.name),
        pluck('name'),
        toArray(),
      )
      .subscribe((res) => {
        console.log(res);
        this.data = res;
      });

    // Ex - 02 for Nested 
    from(this.users)
      .pipe(
        // map(data => data.name),
        pluck('job', 'title'),
        toArray(),
      )
      .subscribe((res) => {
        console.log(res);
        this.data2 = res;
      });