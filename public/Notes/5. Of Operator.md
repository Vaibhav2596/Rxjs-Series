## Of Operator

It converts an argument to an Observable Stream

## From Operator

Creates an Observable from an Array, an array like object, a Promise, an iterable object, or an Observable-like object.