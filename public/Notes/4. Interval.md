## Interval
    Interval is operator which keeps on getting called after a particular time of interval

## Timer

    Timer is same as interval but it has extra delay parameter
    // timer(delay, interval);